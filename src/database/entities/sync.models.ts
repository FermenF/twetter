import { User } from '../../models/User';
import { Posts } from '../../models/Posts';
import { Posts_Likes } from '../../models/Posts_Likes';

export const Entities = [ 
    User, Posts, Posts_Likes
];